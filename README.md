# 가족플랫폼

가계부, 여행 기록, 일기/육아 기록, 가족 공유 기능을 붙여가는 React + Vite 웹앱입니다.

## 실행

```bash
npm install
npm run dev
```

## 빌드

```bash
npm run build
```

## 지도

여행 기록의 위치 지도는 OpenStreetMap 기반으로 표시합니다.
별도 유료 API 키 없이 지도 위에 여행 순서 마커와 연결선을 보여줍니다.
